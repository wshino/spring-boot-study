spring:
  thymeleaf:
    cache: false
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/tmp/springbootWhiskyList
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
